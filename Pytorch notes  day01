
当你想将文本格式化为更好看的形式时，使用Markdown是一个不错的选择。Markdown是一种轻量级标记语言，用于简单而直观地格式化文本。以下是你的笔记的Markdown格式示例：

markdown
Copy code
# 安装 Anaconda

1. 安装 Anaconda，一个集成了多样的研究工具包的软件。保存好安装路径，一路默认安装，打开 Anaconda Prompt。如果默认出现的为（base），那么就是安装成功。

2. 安装显卡驱动 + conda 工具包。检查显卡驱动是否正确安装：使用任务管理器，检查 GPU 是否正确显示型号。如果正确显示型号，那么显卡驱动就是正确安装的。

3. 安装 PyTorch 之前，学习如何正确管理环境。由于项目有着不同的环境，比如一些基于 PyTorch 0.4，另一些基于 1.0，我们需要正确的管理环境来分别对不同的项目进行运行和管理。

```bash
(base) C:\Users\21293>conda create -n pytorch python=3.6
激活环境：

bash
Copy code
(base) C:\Users\21293>conda activate pytorch
激活环境后用 pip list 检查包配置环境：

bash
Copy code
(pytorch) C:\Users\21293>pip list
Package      Version
------------ ---------
certifi      2021.5.30
pip          21.2.2
setuptools   58.0.4
wheel        0.37.1
wincertstore 0.2
安装 conda，激活 PyTorch 环境。PyTorch 官网选择适合自己系统的 PyTorch 版本。由于环境是 Python 3.6，所以选择的 conda 版本是 1.4，其 CUDA 版本是 9.2。
bash
Copy code
conda install pytorch==1.4.0 torchvision==0.5.0 cudatoolkit=9.2 -c pytorch
注意： 最开始使用的是 Anaconda 3.5、Python 3.6、PyTorch 1.4 的版本。然而，在执行 >>> torch.cuda.is_available() 时报错 False。在更新 PyTorch、Python 以及 CUDA 版本后依然没有解决。最终尝试更新 Anaconda 版本解决了该问题。不过，我不理解的是 Anaconda 本身不过是一个包管理工具，为什么它本身会影响这个功能。

询问 ChatGPT 给出的答复是：Anaconda 本身的版本较老可能导致 torch.cuda.is_available() 返回 False 的情况，尤其是在使用较新的 PyTorch 版本时。torch.cuda.is_available() 用于检查系统是否支持 CUDA（NVIDIA GPU 加速），而 CUDA 的支持通常需要与安装的 PyTorch 版本相匹配的 NVIDIA 驱动程序和 CUDA 工具包。更新 Anaconda 本身并不直接影响 torch.cuda.is_available() 的结果，但如果你使用的 PyTorch 版本要求较新的 NVIDIA 驱动程序和 CUDA 工具包，而你的 Anaconda 版本又较老，那么这可能导致 torch.cuda.is_available() 返回 False。

这里使用了Markdown的语法，包括标题、列表、引用等，以更好地呈现文本。你可以将上述Markdown代码复制到支持Markdown的编辑器中查看效果。




